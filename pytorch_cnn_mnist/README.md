# PyTorch CNN MNIST

Use the root-level instructions (`README.md`) for setup.  
From the repo root you can train with:
```bash
python pytorch_cnn_mnist/train.py
```
The folder just holds the CNN code and the checkpoint (`cnn_mnist.pt`) if you save one.
